services:
  quarto:
    image: ghcr.io/quarto-dev/quarto:latest
    working_dir: /project
    volumes:
      - .:/project
    ports:
      - "4200:4200"
    command: >
      quarto preview
      --host 0.0.0.0
      --port 4200
      --watch-inputs